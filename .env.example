ACCEPT_EULA=Y
# SA_PASSWORD=ChangeMe!ReplaceWithSecurePassword

# Example environment variables (do NOT commit your real secrets)
# SA_PASSWORD should be set locally and kept out of version control
# SA_PASSWORD=ReplaceWithStrongPassword

DB_HOST=queueboard-db-1
DB_PORT=1433
DB_USER=sa

# Optional: full connection string alternative
# DEFAULT_CONNECTION=Server=${DB_HOST},${DB_PORT};User Id=${DB_USER};Password=${SA_PASSWORD};TrustServerCertificate=True;
